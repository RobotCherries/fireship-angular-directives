// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyDscABSRPBJ2dnR_awi0hYArE5Ugh_rv-A',
    authDomain: 'fireship-angular-directives.firebaseapp.com',
    databaseURL: 'https://fireship-angular-directives.firebaseio.com',
    projectId: 'fireship-angular-directives',
    storageBucket: 'fireship-angular-directives.appspot.com',
    messagingSenderId: '324370539429',
    appId: '1:324370539429:web:166888b733acdd17f03e4c'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
